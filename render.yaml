services:
  - type: web
    name: pdx-reviewdesk
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: REVIEWDESK_RUNS_DIR
        value: /tmp/reviewdesk-runs
